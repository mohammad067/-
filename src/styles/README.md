# Application Styles & Tokens (`src/styles/`)

This directory houses our main styling assets, global CSS sheets, and design system variables.

## Contents
- `globals.css`: Implements Tailwind CSS base imports, `@theme` directives, global animation frames, and light/dark theme CSS variables.
