# Server Authentication (`src/server/auth/`)

This directory contains the configurations and middleware helpers driving system security.

## Responsibility
- Define cookie parser and session tokenizer wrappers.
- Extract authentication properties from incoming HTTP headers.
- Handle role verification and route shielding limits.
