# Product Catalog Feature (`src/features/product-catalog/`)

This feature handles browsing, searching, filtering, and detailed inspection of premium Iranian rice varieties.

## Responsibility
- Implement listing pages, search inputs, and luxury filters (filtering by type, package size, region).
- Connect to server-state using TanStack Query hooks.
- Manage filtering UI states within a feature-scoped Zustand store.
