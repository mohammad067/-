# Cart Feature (`src/features/cart/`)

This feature manages the temporary storage and modification of customer selection before checkout begins.

## Responsibility
- Provide localized client-side quantity modifiers and pricing calculations.
- Display the dynamic slide-out luxury Cart Drawer overlay.
- Keep the local UI-state synced with server database sessions where appropriate using custom hooks.
